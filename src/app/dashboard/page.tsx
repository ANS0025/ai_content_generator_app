import GeneratorList from "@/components/generator-list";

const Dashboard = () => {
  return (
    <section className="px-10">
      <GeneratorList />
    </section>
  );
};

export default Dashboard;
